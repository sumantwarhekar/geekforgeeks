import java.util.*;

class Solution {
    static List<Integer> get(int a, int b) {
        List<Integer> answer = new ArrayList<Integer>();
        answer.add(b);
        answer.add(a);
        return answer;
    }
}